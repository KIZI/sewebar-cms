It is simple test if my implementation of schematron works.

There is file testSchematron.php. If you run this file in web browser, you should
see page containing empty textarea. If there is anything inside it, it means
thar ARQuery-Sample.xml is wrong.